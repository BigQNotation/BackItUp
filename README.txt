This program is run from a Linux terminal, tested in an Ubuntu environment.

Compile: "make"
To run in backup mode: "./BackItUp"
To run in restore mode: "./BackItUp -r"
If a permission denied error occurs: "chmod +w .backup", "chmod +r .backup", and "chmod +x .backup" before running the program